# Required — obtain your API key from https://aistudio.google.com/apikey
GEMINI_API_KEY="Your API key"

# Optional — number of top chunks to retrieve per query (integer 1–20, default 5)
TOP_K_CHUNKS=5
